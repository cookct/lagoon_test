# Memory — 2026-04-19

## [context] Note
confidence: 1.0
[BUG FIX] Video/Image Mode Sidebar Not Updating: When switching modes, refreshParameterPanel() runs synchronously before populateSelect() completes its async import. Setting selectEl.value programmatically doesn't fire 'change' events. Fix: Call refreshParameterPanel() inside the async callback AFTER populateSelect() finishes. File: js/ui/settings.js lines 1903-1909. Same pattern applies to both videoModeManager and imageModeManager.
---

## [gotcha] Inline styles override CSS !important in specific cases
confidence: 0.85
When `refreshParameterPanel()` sets `style.display = 'block'` on video cards, it can override CSS `display: none !important` on parent containers because the cards themselves lack the mode-specific class. The parent container's visibility doesn't cascade properly when children have inline styles.
---

## [architecture] Video params panel is sibling of right-sidebar-content
confidence: 0.9
The `video-params-panel` is a direct child of `.sidebar-right`, NOT inside `#right-sidebar-content`. Same for `target-card-container` and `image-generate-panel`. This was intentional so hiding `rightContent` doesn't affect them.
---

## [gotcha] refreshParameterPanel bleeds into image mode
confidence: 0.95
The function `refreshParameterPanel()` was being called in image mode and setting inline display styles on video cards, causing video UI elements to appear in image mode. Guard clause `if (state.mode !== 'video') return;` was attempted but didn't fully resolve the issue.
---

## [context] Venice video API has no safe_mode parameter
confidence: 0.9
The Venice `/video/queue` endpoint has no `safe_mode`, `nsfw`, or content filter toggle. Available params: model, prompt, duration, negative_prompt, aspect_ratio, resolution, upscale_factor, audio, image_url, end_image_url, audio_url, video_url, reference_image_urls, elements, scene_image_urls.
---

## [decision] Seedance 2.0 R2V config: no negative prompt field
confidence: 0.95
User specified Seedance should only have duration, resolution, variants, and audio controls. Negative prompt was incorrectly added by copy-paste from other models and removed.
---

## [context] Financial constraints affecting AI usage
confidence: 0.95
User has $14 remaining balance and no big jobs for 10 days, currently limiting themselves to chat models (cheaper) rather than video generation to conserve credits.
---

## [context] User writes HP fanfiction on AO3
confidence: 0.95
User is a Harry Potter fanfiction writer on Archive of Our Own, focusing on psychological trauma and recovery narratives centered on Hermione Granger.
---

## [decision] Threesome healing arc challenge
confidence: 0.9
User was challenged by an AO3 reader to write a Hermione/Harry/Ginny threesome that heals Hermione, with the constraint that it must feel inevitable and like 'the only way' - not gratuitous.
---

## [context] Related fic structure
confidence: 0.95
User has two connected fics: 'The Long March to the Forest' (trauma/deconstruction, Major Character Death warning) and 'The Color of His Eyes' (healing aftermath, Explicit rating, contains the threesome arc).
---

## [preference] Values uncensored AI models
confidence: 0.85
User prefers uncensored AI platforms (praised Grok for 'giving no fucks' about adult content) and is frustrated by tiered censorship systems that produce Barbie-doll anatomy.
---
