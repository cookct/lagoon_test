# Memory — 2026-05-19

## [gotcha] Kodi parallel build race condition
confidence: 0.9
Parallel builds with many jobs can fail due to race conditions. Using -j2 instead of higher job counts resolves this.
---

## [decision] Confluence skin as default
confidence: 0.95
Estuary skin was replaced with Confluence as the default skin for this Kodi build.
---

## [progress] Repeat button added to VideoOSD
confidence: 0.95
A repeat button with Off/One/All states was added to VideoOSD.xml in the Confluence skin.
---

## [gotcha] CMake GL vs GLES requirement
confidence: 0.85
CMake configuration fails at Platform.cmake:11 if GL or GLES rendering is not explicitly specified.
---

## [gotcha] edit_file searches disk not staged buffer
confidence: 0.95
During transaction buffering, search and read operations see the disk file content, not the staged/buffered changes. Edits apply to staged content but verification requires committing first.
---

## [architecture] context_mode is frontend-side only
confidence: 0.9
context_mode (and system_context) are handled entirely frontend-side. chat.py and chats.py pass the config dict through as-is, so new config fields are automatically persisted without backend changes.
---

## [pattern] New config fields require multiple sync points
confidence: 0.85
When adding new config fields like context_mode, must add to: defaultChatConfig in state.js, loadChat overlay in ChatManager.js, and the mobile.js field list for mobile support.
---

## [decision] RAG mode requires re-embedding on mode switch
confidence: 0.9
When user switches context_mode from 'always' to 'rag', they must re-embed their context file. Embedding is also triggered when saving a new config in RAG mode if not already done.
---

## [architecture] contextControls ID replaced contextBtn
confidence: 0.85
The UI for context controls changed from contextBtn ID to contextControls/system_context_controls div structure to support RAG mode embedding controls.
---
